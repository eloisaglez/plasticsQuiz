import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './plasticsQuiz.jsx' // Asegúrate de que aquí el nombre coincida con tu archivo (si pusiste p minúscula)

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
